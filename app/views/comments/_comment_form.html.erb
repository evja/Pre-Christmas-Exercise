<div class="form"><%= bootstrap_form_for [@link, @comment] do |f| %>

  <%= f.text_field :author, :value => "#{current_user.email}" %>

  <%= f.text_field :body, control_col: "col-sm-3" %>

  <%= f.submit %>

<% end %></div>